# Sytex Excel Form Template Workflow

Guidance for working with Sytex Excel form templates (`.xlsx`) safely:

- analyze OpenXML structure
- map text and image references
- validate references against real form JSON
- apply targeted fixes without breaking neighboring blocks

Core workflow details live in `SKILL.md`.
