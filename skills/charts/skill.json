{
  "name": "charts",
  "title": "Charts",
  "description": "Generate charts and graphs from data for reports. Supports bar, line, pie, scatter, and histogram charts.",
  "version": "1.1.0",
  "setup_instructions": "This skill requires uv. matplotlib is declared inline in the script and resolved by `uv run --script`, so no interpreter-level install is needed.",
  "dependencies": [
    {
      "name": "uv",
      "check": "uv --version",
      "install": {
        "darwin": "curl -LsSf https://astral.sh/uv/install.sh | sh",
        "linux": "curl -LsSf https://astral.sh/uv/install.sh | sh",
        "windows": "winget install astral-sh.uv"
      }
    }
  ],
  "fields": [],
  "test_command": "help",
  "files": ["charts", "SKILL.md", "README.md"]
}
