{
  "name": "charts",
  "title": "Charts",
  "description": "Generate charts and graphs from data for reports. Supports bar, line, pie, scatter, and histogram charts.",
  "version": "1.0.0",
  "setup_instructions": "This skill requires Python 3 and matplotlib. The installer will check and install matplotlib if needed.",
  "dependencies": [
    {
      "name": "python3",
      "check": "python3 --version",
      "install": {
        "darwin": "brew install python3",
        "linux": "sudo apt-get install -y python3 || sudo yum install -y python3",
        "windows": "winget install Python.Python.3"
      }
    },
    {
      "name": "matplotlib",
      "check": "python3 -c 'import matplotlib'",
      "install": {
        "darwin": "pip3 install matplotlib",
        "linux": "python3 -m pip install matplotlib",
        "windows": "pip install matplotlib"
      }
    }
  ],
  "fields": [],
  "test_command": "help",
  "files": ["charts", "SKILL.md", "README.md"]
}
